Ctrl + P 点出参数类型
Ctrl + Shift + N 查找类
Alt + 7 显示类中的方法
Ctrl + H 查看当前接口的层次结构图(hierarchy), Ctrl+h快捷键可查看
Ctrl + Alt + B 查看接口的实现类

向9999端口发送字符串nc -lk 9999 输入字符 https://blog.csdn.net/maxueke2017/article/details/124674081
windows
nc -lp 1234 发送数据
nc -nv 127.0.0.1 1234 接受数据

测试4

1.	union之前两个流的类型必须是一样，connect可以不一样
2.	connect只能操作两个流，union可以操作多个。
3.Temporal join 事件时间结论：触发数据写出的条件：左流的水位线提升了，并且右流的水位线大于等于左流的水位线，在触发之前，左右流的join结果缓存在内存中，
如果停掉程序进行重新一次性读取，两边会按照时间线顺序有序join；若左流的join某id结果已输出，更改右流该id时间不会触发重新join，除非左流重新输入该id。